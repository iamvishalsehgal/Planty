{
  "name": "planty",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:backend": "cd backend && uvicorn main:app --reload --port 8000"
  },
  "devDependencies": {
    "playwright": "^1.61.0",
    "vite": "^6.3.5"
  }
}
